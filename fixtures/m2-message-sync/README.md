# M2 首发 study 夹具

领域：消息发送与状态同步。

核心类型只有 **Surface / Control / Journey / Observation**。  
发送入口在范围内。穷尽导航树、创建容器、容器管理、穷尽私信、话题串、管理后台在范围外。

产品级 Rocket.Chat 证明是后续在操作者机器上对真实工作区的可信运行，不是 CI。  
CI 使用通用多组件夹具。示例 locator 不是核心类型。
