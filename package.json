{
  "name": "behavior-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "行为地图：通用产品行为分析器（M0 合约 + M1 Source 快照 + M2 detect/plan + M3 start/stop）",
  "license": "MIT",
  "workspaces": [
    "packages/contracts",
    "packages/agent",
    "packages/export",
    "packages/source",
    "packages/project"
  ],
  "scripts": {
    "test": "vitest run && tsc --noEmit -p tsconfig.json",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.13.10",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
