{
  "name": "behavior-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "行为地图：通用产品行为分析器（M0–M9：合约、Source、detect/plan、start/stop、Discovery、真实 AgentRunner、人工审定、四份导出、官方闭环、study runner、创建面与列表确认）",
  "license": "MIT",
  "workspaces": [
    "packages/contracts",
    "packages/agent",
    "packages/export",
    "packages/source",
    "packages/project",
    "packages/discovery",
    "packages/review"
  ],
  "scripts": {
    "test": "vitest run && tsc --noEmit -p tsconfig.json",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.13.10",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
