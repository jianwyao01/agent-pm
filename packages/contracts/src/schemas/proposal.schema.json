{
  "$id": "https://behavior-map.dev/schemas/proposal.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "proposal",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "task_id",
    "run_id",
    "inputs",
    "proposed_journeys",
    "proposed_effects"
  ],
  "properties": {
    "schema_version": { "const": "0.1.0" },
    "id": { "type": "string", "minLength": 1 },
    "task_id": { "type": "string", "minLength": 1 },
    "run_id": { "type": "string", "minLength": 1 },
    "inputs": { "type": "array", "items": { "type": "string" } },
    "kind": {
      "enum": ["classify_features", "build_journeys", "analyze_effects", "prune_candidates"]
    },
    "proposed_features": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "candidate_ids", "evidence_refs", "explanation"],
        "properties": {
          "name": { "type": "string" },
          "candidate_ids": { "type": "array", "items": { "type": "string" } },
          "evidence_refs": { "type": "array", "items": { "type": "string" } },
          "explanation": { "type": "string", "minLength": 1 }
        }
      }
    },
    "proposed_journeys": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "candidate_ids", "effect_candidate_ids"],
        "properties": {
          "name": { "type": "string" },
          "candidate_ids": { "type": "array", "items": { "type": "string" } },
          "effect_candidate_ids": { "type": "array", "items": { "type": "string" } },
          "evidence_refs": { "type": "array", "items": { "type": "string" } },
          "explanation": { "type": "string" }
        }
      }
    },
    "proposed_effects": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "candidate_id", "observation_kind", "observed"],
        "properties": {
          "name": { "type": "string" },
          "candidate_id": { "type": "string" },
          "observation_kind": {
            "enum": [
              "current_surface",
              "other_surface",
              "collection",
              "indicator",
              "notification",
              "backend_operation"
            ]
          },
          "subtype": { "type": "string" },
          "transport": {
            "enum": ["http", "websocket", "rpc", "event", "database", "unknown"]
          },
          "observed": { "type": "boolean" },
          "evidence_refs": { "type": "array", "items": { "type": "string" } },
          "explanation": { "type": "string" }
        }
      }
    },
    "pruned": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["candidate_id", "reason"],
        "properties": {
          "candidate_id": { "type": "string", "minLength": 1 },
          "reason": { "type": "string", "minLength": 1 }
        }
      }
    },
    "proposed_run_plan": { "type": "object" }
  }
}
